import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#45b491",
    },
    secondary: {
      main: "#ffffff",
    },
    icon: {
      main: "#ffffff",
    },
    text: {
      primary: "#5E6A6B",
    },
  },
});

export default theme;
